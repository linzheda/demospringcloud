<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.UserMapper">
    <select id="login" parameterType="User" resultType="com.linzd.pc.entity.User">
		select * from user where name=#{name} and password=#{password}
	</select>


    <insert id="save" parameterType="User">
	    insert into user(name,password,registerTime,updateTime) values(#{name},#{password},now(),null)
   </insert>

    <select id="findById" parameterType="User" resultType="User">
		select * from user where id=#{id}
	</select>


    <update id="update" parameterType="User">
        update user
        <set>
            <if test="name!=null">
                name=#{name},
            </if>
            <if test="password!=null">
                password=#{password},
            </if>
            updateTime=now()
        </set>
        <if test="id!=null">
            where id=#{id}
        </if>
    </update>

    <delete id="deleteById" parameterType="User"><!-- uid=1是用户这个角色不能删除 -->
  		delete from user where id=#{id} and id!=1
  	</delete>

    <select id="test" resultType="map" >
        select * from user where id=2
    </select>

</mapper>